export enum SystemEnv {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  STAGING = 'staging',
}

export enum SystemDatabase {
  POSTGRES = 'postgres',
  REDIS = 'redis',
}

export enum SystemDatabaseProvider {
  NEON = 'neon',
  LOCAL = 'local',
  REDIS_CLOUD = 'redis-cloud',
}
